# Full requirements for the pipeline container (includes ML dependencies).
# Used by Pipeline.Dockerfile - heavy image, only built when running ML jobs.

-r requirements-api.txt

# ML / Sentiment analysis
transformers
torch; sys_platform != 'linux' or platform_machine != 'aarch64'
xgboost
scikit-learn==1.5.0
datasets
matplotlib
papermill
