fastapi==0.95.2
uvicorn==0.22.0
pandas==2.0.3
numpy>=1.24.0,<2.0.0
scikit-learn==1.3.2
sqlalchemy==1.4.41
psycopg2-binary==2.9.6
pydantic==1.10.9
python-dotenv

# run notebooks from the backend container:
papermill
yfinance

# For sentiment analysis
transformers
# Jetson (arm64/aarch64) should use the CUDA-enabled PyTorch that comes from the
# NVIDIA L4T PyTorch base image. On non-aarch64 platforms, install torch via pip.
torch; platform_machine != "aarch64"
xgboost

# Authentication packages (Connor - Auth system)
python-jose[cryptography]
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator
httpx==0.24.1

# Testing (unit + behavioral)
pytest
behave
