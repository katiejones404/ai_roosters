# Stocks test configuration
# Path setup is handled by Testing/conftest.py (repo root).
# Behavioral fixtures (FastAPI TestClient + sqlite_session) live in
# Testing/Stocks/behavior/conftest.py so they do not break the unit tests.
